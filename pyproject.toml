[project]
name = "shared-themes"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "pyyaml",
]

[dependency-groups]
dev = [
    "mypy",
    "pylint",
    "pytest",
    "ruff",
]

[tool.uv]
# Demo/teaching repo: not an installable package.
package = false
